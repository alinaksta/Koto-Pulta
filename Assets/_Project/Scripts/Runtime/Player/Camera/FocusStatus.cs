namespace Game.Player
{
    /// <summary>
    /// Describes the camera's current focus state.
    /// </summary>
    public enum FocusStatus
    {
        Unfocused,
        InTransition,
        Focused
    }
}
